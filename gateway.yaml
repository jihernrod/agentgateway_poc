apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: basic-agent-gateway
  namespace: agentgateway-ingress
spec:
  gatewayClassName: agentgateway
  listeners:
  - name: http
    protocol: HTTP
    port: 8080
    allowedRoutes:
      namespaces:
        from: Same
